# Optional — enables the AI-enhanced recommendation endpoint (/api/recommend/ai).
# Without it, the app still works fully on the rule-based engine.
# Get a key at https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Optional — port the API listens on (default 4000)
PORT=4000
